apiVersion: v1
kind: Service
metadata:
  name: textile-app-service
  labels:
    app: textile-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 3001
    nodePort: 30080
  selector:
    app: textile-app
